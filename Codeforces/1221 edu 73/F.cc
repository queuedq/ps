#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

#define MAX_N 500000

int n;
ll x[MAX_N], y[MAX_N], c[MAX_N];

void input() {
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> x[i] >> y[i] >> c[i];
  }
}

void solve() {
  for (int i = 0; i < n; i++) {

  }
}

int main() {
  input();
  solve();

  return 0;
}
