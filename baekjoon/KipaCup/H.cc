#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
typedef long long lld;
typedef pair<int, int> pii;
typedef pair<lld, lld> pll;

////////////////////////////////////////////////////////////////


int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  ////////////////////////////////

  string s; cin >> s;

  if (s == "A") cout << 8 << endl;
  if (s == "B") cout << 7 << endl; //
  if (s == "C") cout << 5 << endl;
  if (s == "D") cout << 2 << endl; //
  if (s == "E") cout << 4 << endl;
  if (s == "F") cout << 9 << endl; //
  if (s == "G") cout << 12 << endl; //

  ////////////////////////////////
  return 0;
}
